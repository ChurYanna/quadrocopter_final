# SFSC 驱动的多无人机动态混合障碍协同通行算法总结

## 1. 算法定位

本项目目前已经从单纯的多无人机仿真 demo，基本收敛为一套“传感器融合结构化上下文（SFSC）+ 前视 RGB + 多模态 VLM 高层候选策略 + 安全验证/时效性门控/安全投影 + 底层稳定控制”的多无人机动态混合障碍通行框架。

当前最终版定位不是“VLM 直接控制无人机”，而是“VLM 在认知层提出 PassagePlan 候选，确定性安全系统负责验证、投影和执行”。因此算法可以包装为：

```text
Safety-Projected Multimodal VLM Planning for Cooperative Multi-UAV Passage
```

中文表述为：

```text
面向多无人机协同通行的安全投影多模态 VLM 策略规划框架
```

当前稳定基准场景以 `tests/test_dynamic_mixed_bypass_obstacle_zone.py` 为准。该场景包含动态穿越型障碍和实体绕行型障碍，已经验证了算法的核心闭环：

- 动态窄洞/窄门：采用蛇形时序穿越。
- 实体障碍：采用侧绕或越顶。
- 穿越与绕行混合：不强制重组编队，而是在局部障碍之间维持可执行的单机目标和队列约束。
- VLM：不直接控制无人机，而是读取前视 RGB + SFSC 上下文、生成高层通行候选策略，并通过安全验证、返回时效性门控和安全投影后进入执行缓冲区。

## 2. 核心算法框架

算法可以抽象为五层。

### 2.1 SFSC 传感器融合结构化上下文编码层

SFSC 是 Sensor-Fused Structured Context 的缩写，中文可表述为“传感器融合结构化上下文”。它不是单纯的视觉描述，也不是直接读取 XML 或仿真底层变量，而是将非视觉传感器和系统状态经过快速编码后形成的高层可规划上下文。

在真实系统中，SFSC 的来源包括：

- IMU、里程计、VIO/定位模块提供的位姿和运动状态。
- 雷达、测距、LiDAR 或深度传感提供的距离与障碍接近信息。
- 多无人机通信提供的机间相对位置、队形宽度和可用通行顺序。
- 控制器与规划器提供的当前阶段、速度能力、已规划/已清障状态。
- 快速非 VLM 感知模块提供的障碍几何估计和运动趋势。

在当前 MuJoCo demo 中，为了可复现和便于实验验证，SFSC 由仿真运行时状态和场景配置构造；这对应真实系统中的传感器融合输出，而不是论文意义上的“直接读取答案”。

多模态扩展中，视觉模态不再使用上帝视角或由结构化上下文重新绘制的语义图，而是使用当前 x 方向最前方 UAV 的机载前视 RGB 关键帧。这样视觉分支保留探路无人机看到的原始外观证据，SFSC 分支提供非视觉传感器融合后的精确约束，两者形成互补输入。

每个障碍不再只是坐标和尺寸，而被描述为：

- 障碍类型：动态孔洞、实体箱体、宽体障碍、高障碍等。
- 功能属性：可穿越型 `aperture` 或实体绕行型 `solid`。
- 几何语义：位置、洞口宽度、通过高度、实体尺寸。
- 运动语义：静态、横向周期运动、运动幅值、频率。
- 风险语义：低、中、高风险。

这一步是 LLM 接入的基础，因为 LLM 不读取 MuJoCo 原始状态，而读取“由传感器融合得到、可被模型理解、也能被控制系统验证”的 SFSC 上下文。

### 2.2 可通行性分析层

由 `src/passage_planning/passability.py` 完成。

核心判断包括：

- 当前编队是否能整体通过洞口。
- 单机蛇形是否能通过洞口。
- 障碍是否必须绕行或越顶。
- 推荐策略模式：`formation`、`snake_sequence`、`bypass`。

这一层相当于给 LLM 和确定性规划器提供一个低层物理可行性的先验。

### 2.3 时空协同策略规划层

由 `src/passage_planning/strategy_planner.py` 输出 `PassagePlan`。

核心内容包括：

- 多无人机通行顺序。
- 单机时间槽。
- 每个障碍的策略模式。
- 每个障碍的观察点、释放点和清障点。

这里是算法的核心创新之一：队伍不是简单避障，而是被组织成“时空队列”。特别是动态窄洞中，无人机不是一起挤过去，而是按照时序槽逐个穿越，从而降低碰撞和擦障风险。

### 2.4 安全验证层

由 `src/passage_planning/strategy_validator.py` 负责。

该层已经从早期的“格式验证”升级为“可执行性验证”。它不只检查 JSON 字段是否存在，还检查：

- 通行顺序是否合法。
- 时间槽是否覆盖所有无人机。
- 策略模式是否与障碍类型匹配。
- 观察窗口和清障窗口是否合理。
- LLM 输出是否能被底层执行器消费。

LLM 生成的策略必须通过该验证层。否则系统会采用确定性 fallback，不允许 LLM 直接把不安全策略注入控制层。

### 2.5 稳定执行控制层

当前稳定执行内核由 `tests/test_three_snake_dynamic_gates.py` 中的 `run_stable_passage_kernel()` 提供，并通过 `tests/stable_passage_demo_kernel.py` 暴露给 demo。

执行层负责：

- 起飞后编队接近障碍区。
- 在观察点前解散编队，转为蛇形通行。
- 动态预测洞口中心。
- 根据时间槽限制 x 方向前进速度。
- 根据洞口中心控制 y/z 对齐。
- 对实体障碍执行侧绕或越顶。
- 全部通过最后障碍后恢复编队。

后续新 demo 应只提供配置和 XML 场景，不应修改该内核。

## 3. LLM 如何介入

当前 LLM 的定位是“高层语义决策器”，不是飞控器。

### 3.1 LLM 输入什么

LLM 接收的是 SFSC 传感器融合结构化上下文，而不是 MuJoCo 原始状态。

典型输入包括：

- 无人机数量、队形宽度、安全半径。
- 当前有限视野中看到的障碍列表。
- 每个障碍的类型、尺寸、位置、运动模式、洞口高度、洞口宽度。
- 当前任务偏好，例如快速安全通行、允许临时解散、最后恢复编队。
- 确定性规划器给出的基准策略。

这能回答导师关注的问题：环境内上下文不是凭空来的，而是由非视觉传感器、机间通信、状态估计和快速感知模块融合编码得到。仿真中的几何和运行时状态只是这一传感器融合过程的可复现实验替代。

### 3.2 LLM 输出什么

LLM 输出高层策略 JSON，主要包括：

- 全局策略模式。
- 每架无人机的通行顺序。
- 时间槽间隔。
- 每个障碍的策略：蛇形穿越、侧绕、越顶、保持等待等。
- 每个障碍的观察点和清障点。

输出后必须经过安全验证器。验证通过才会进入在线策略缓冲区。

### 3.3 在线实时接入

当前已经加入有限视野在线重规划机制：

- 无人机不是一开始把全地图都交给 LLM。
- 系统按前向感知窗口逐步发现障碍。
- 当新障碍进入详细感知范围时，触发局部语义重规划。
- 为避免 viewer 卡顿，确定性 fallback 会立即给出可执行策略。
- Qwen 等在线 LLM 请求异步执行，返回后通过验证器再合并到策略缓冲区。

因此实时 LLM 的作用不是每一帧控制，而是事件触发式地更新高层策略。

## 4. LLM 如何呈现

导师强调的重点是：不能只说用了 LLM，而要让人直观看到 LLM 在做什么。

当前可视化已经围绕“SFSC + LLM 证据链”设计：

- SFSC 上下文形成：显示非视觉传感器融合后系统得到什么障碍和队形约束。
- 输入给 LLM 的 SFSC：显示模型收到的结构化上下文。
- LLM 高层策略输出：显示模型建议的通过顺序、时间槽和障碍策略。
- 安全验证结果：显示该策略是否被接受、是否修复、是否 fallback。
- 在线感知窗口：显示飞行过程中当前看到了哪些障碍。
- 异步重规划事件：显示 fallback 先执行、LLM 后返回的过程。
- 底层执行反馈：显示每架无人机当前在穿越、绕行、等待还是恢复编队。

这使得 LLM 不再是隐藏在代码里的黑箱，而是以“SFSC 输入 - 策略输出 - 验证 - 执行”的链路被实时展示。

面向导师或论文展示时，建议录屏中同时展示：

- MuJoCo viewer 中无人机和障碍物运动。
- 语义面板中 LLM 的输入、输出和验证。
- 终端中最终 metrics，例如总耗时、最小机间距、最小洞口净空、是否 fallback。

## 5. 相比传统方法的特点

传统多无人机避障常见路线包括：

- 人工势场、速度障碍、MPC、采样规划、强化学习。
- 优点是数学闭环明确。
- 缺点是遇到混合语义障碍时，需要人为写大量规则，难以解释“为什么该穿洞、为什么该侧绕、为什么该越顶”。

本算法的差异在于：

- 用 SFSC 层统一组织非视觉传感器和队形状态。
- 用 LLM 做高层策略解释和组合决策。
- 用确定性验证器保障可执行性。
- 用底层稳定控制器保障飞行连续性。
- 用可视化面板把语义决策链展示出来。

因此它不是简单“LLM 控制无人机”，而是“LLM 参与多无人机协同通行策略生成，确定性系统负责约束和执行”。

## 6. 当前已完成工作

当前算法已经基本成型，完成内容包括：

- 动态窄洞蛇形时序穿越。
- 动态实体障碍侧绕和越顶。
- 穿越式障碍与绕行式障碍混合。
- LLM 策略 JSON 接口。
- 安全验证器。
- 有限视野在线感知。
- 异步在线 LLM 重规划。
- 无人机机载前视 RGB 关键帧记录。
- 实时 SFSC + LLM 证据链面板。
- 稳定 demo 执行内核抽象。
- 稳定基准 demo：`tests/test_dynamic_mixed_bypass_obstacle_zone.py`。

## 7. 当前主要不足

当前不足不在算法主线，而在工程鲁棒性：

- 新场景一旦过度复杂，容易暴露底层控制细节问题。
- 复杂实体形状目前主要通过语义配置表达，底层执行策略仍以侧绕/越顶为主。
- 多孔墙、斜洞、弯曲通道等复杂结构还没有真正对应到多候选路径求解器。
- LLM 对复杂障碍的策略可以展示，但执行器目前只支持有限策略集合。

因此后续不应继续把所有复杂形状直接塞进稳定 demo，而应逐步扩展“语义策略类型”和“底层可执行动作库”。

## 8. 下一阶段建议

建议按三步推进：

1. 固定稳定 demo，作为论文主算法演示基准。
2. 围绕稳定 demo 做系统实验：无 LLM、有 LLM、无验证器、有验证器、同步 LLM、异步 LLM。
3. 逐步扩展复杂障碍库，每次只新增一种可执行策略，例如多孔选择、底穿、斜洞跟踪、弯曲通道。

这样论文逻辑会更清楚：先证明框架有效，再证明语义复杂度提升时 LLM 具备价值。
