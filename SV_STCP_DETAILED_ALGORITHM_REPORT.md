# SV-STCP：语义验证的多无人机动态混合障碍时空协同通行算法报告

## 0. 报告定位

本文只讨论当前算法本身，不讨论项目包装、界面宣传或泛泛的 LLM 概念。

当前算法可以命名为：

```text
SV-STCP: Semantics-Verified Spatio-Temporal Cooperative Passage
```

中文可表述为：

```text
语义验证的多无人机动态混合障碍时空协同通行算法
```

核心思想是：将复杂障碍区先转化为可解释的语义对象，再由 LLM 参与高层通行策略生成，由安全验证器约束 LLM 输出，由稳定底层执行器完成蛇形时序穿越、实体绕行、顶部越障和恢复编队。

该算法不是“LLM 直接控制无人机”，而是“LLM 作为语义策略层，与确定性验证和控制层协同工作”。这一点是本文最重要的技术边界。

当前稳定基准 demo 为：

```text
tests/test_dynamic_mixed_bypass_obstacle_zone.py
```

该 demo 是目前算法闭环的主验证场景，后续实验、论文消融和视频展示都应以它为基准。

## 1. 研究问题

### 1.1 任务场景

考虑多架四旋翼无人机从初始横向编队出发，沿任务主方向通过一个动态混合障碍区。障碍区中同时存在两类障碍：

```text
穿越型障碍：窄门、窄洞、动态孔洞、不同高度的墙面开孔
实体型障碍：箱体、高宽障碍、需要侧绕或越顶的实体障碍
```

与普通避障不同，本问题的难点不是单机绕过一个静态障碍，而是：

1. 多架无人机需要在同一个狭窄通道中按时序通过。
2. 孔洞会运动，过洞点不是固定的。
3. 穿越式障碍和绕行式障碍交替出现。
4. 无人机之间会出现追尾、挤洞、横向聚拢碰撞等多机耦合风险。
5. 如果 LLM 在线介入，不能因为等待 LLM 响应而阻塞飞行控制循环。

### 1.2 算法目标

给定无人机编队状态、障碍区语义信息和任务偏好，算法需要输出并执行一套高层通行策略：

```text
输入：
    多无人机数量、初始队形、机体安全半径、参考速度
    障碍类型、位置、尺寸、动态运动模型、孔洞宽度、高度
    任务偏好：快速、安全、允许临时解散、最终恢复编队

输出：
    每个障碍的通行模式
    多无人机通过顺序
    单机时空槽
    每个障碍的观察点、释放点、清障点
    底层执行器可消费的策略序列
```

算法约束包括：

```text
无人机之间不能发生碰撞
无人机不能与障碍物发生碰撞
动态孔洞处需要预测并跟踪洞口中心
实体障碍处需要选择侧绕或越顶
LLM 输出必须经过验证后才能进入执行
在线 LLM 调用不能阻塞 viewer 和控制循环
```

## 2. 总体方法

SV-STCP 的算法流水线如下：

```text
MuJoCo 场景 / 在线感知
        ↓
语义场景编码器
        ↓
可通行性分析
        ↓
确定性基准策略生成
        ↓
LLM 语义策略生成
        ↓
安全验证与 fallback
        ↓
异步在线策略缓冲区
        ↓
稳定底层执行内核
        ↓
语义证据链面板 + MuJoCo viewer 同步展示
```

各层职责严格分离：

```text
语义层：解释环境是什么
策略层：决定应该怎么通过
验证层：判断策略是否能执行
执行层：将策略转化为连续飞行动作
呈现层：展示 LLM 到底看到了什么、想了什么、输出了什么、是否被接受
```

这种架构的核心优势在于：LLM 的强项用于语义理解和策略组合，确定性算法的强项用于约束、安全和连续控制。两者不是相互替代，而是分层协同。

## 3. 语义场景编码

### 3.1 为什么需要语义编码

LLM 不适合直接读取 MuJoCo 的底层状态，例如 body id、geom size、qpos、qvel 或控制器内部误差。直接输入这些数据既不直观，也不利于可解释展示。

因此算法首先将仿真环境转换为结构化语义对象。LLM 看到的是“这个障碍是动态窄洞、洞口宽度是多少、通过高度是多少、是否横向运动、风险等级如何”，而不是一堆底层仿真变量。

这一步直接回答导师关注的第一个问题：

```text
环境内语义信息怎么来？
```

答案是：语义信息由场景编码器从障碍几何、运动模型和无人机队形状态中提取出来，并构造为统一的障碍语义描述。

### 3.2 障碍语义描述

每个障碍被抽象为一个语义描述对象，核心字段包括：

```text
obstacle_id        障碍编号
obstacle_type      障碍细分类别
function           功能类型：aperture 或 solid
x                  障碍中心在任务方向的位置
center_y           障碍横向中心
pass_z             推荐通过高度
aperture_width     孔洞宽度
motion_axis        运动轴
motion_model       运动模型
motion_amplitude   运动幅值
motion_omega       运动频率
motion_phase       运动相位
risk_level         风险等级
size_x/y/z         实体障碍三维尺寸
```

这里的 `function` 是关键抽象：

```text
aperture：穿越型障碍，需要判断编队整体穿越、单机蛇形穿越或绕行
solid：实体型障碍，需要判断侧绕、越顶或等待
```

这种表达使不同形状障碍可以进入同一套策略框架。窄洞、窄门、移动门都可以归入 `aperture`；箱体、长方体、高墙实体都可以归入 `solid`。

### 3.3 动态孔洞语义

动态孔洞不仅有几何尺寸，还包含运动语义。洞口中心可以描述为：

```text
center_y(t) = base_y + A sin(omega * t + phase)
```

如果存在复合运动，还可以扩展为多项正弦叠加。对控制器来说，关键不是障碍“现在在哪里”，而是无人机到达洞口时“洞口会在哪里”。因此该语义层为后续预测穿越提供基础。

### 3.4 实体障碍语义

实体障碍的语义重点是尺寸和绕行可行性：

```text
size_x：障碍厚度，决定清障点要放在多远之后
size_y：障碍横向宽度，决定侧绕距离
size_z：障碍高度，决定是否适合越顶
motion_model：是否动态运动
```

早期 demo 中出现过一个关键问题：实体障碍厚度没有被充分考虑时，无人机还没完全绕过实体，就会被下一个障碍接管，导致提前向中心聚拢或冲向下一扇门。当前算法通过“障碍后缘释放”和“策略缓冲区中的 obstacle clear 判断”避免这种过早切换。

## 4. 可通行性分析

语义编码之后，算法需要判断障碍应该如何通过。可通行性分析并不依赖 LLM，它是确定性物理约束层。

### 4.1 队形宽度与单机宽度

首先计算：

```text
formation_width = max(original_y) - min(original_y) + 2 * UAV_radius
single_file_width = 2 * UAV_radius
```

对于穿越型障碍：

```text
如果 aperture_width >= formation_width + margin:
    推荐保持编队整体通过 formation

如果 aperture_width >= single_file_width + margin:
    推荐蛇形时序穿越 snake_sequence

否则:
    推荐绕行或等待 bypass
```

这使 LLM 和规划器不会凭感觉判断能不能过洞，而是有物理可行性先验。

### 4.2 实体障碍判断

对于实体障碍，默认推荐 `bypass`，再根据尺寸和任务偏好细化为：

```text
高而窄：侧绕优先
宽而矮：越顶或底穿优先
动态实体：提高风险等级，提前观察和释放
厚实体：清障点必须延后
```

当前稳定 demo 中，实体绕行策略已经覆盖侧绕和越顶两种动作；后续若扩展多孔墙、斜洞、底穿等场景，应先扩展“可执行动作库”，而不是直接让 LLM 输出底层不可执行动作。

## 5. 时空协同策略规划

### 5.1 PassagePlan

策略规划器最终输出 `PassagePlan`。其逻辑含义如下：

```text
plan_id                  策略编号
mode                     全局通行模式
passing_order            UAV 通过顺序
time_slot_interval       相邻 UAV 的时间槽间隔
slots                    每架 UAV 的名义时间窗
obstacle_strategies      每个障碍的局部策略
recover_after_last_obstacle 是否在最后恢复编队
confidence               策略置信度
source                   策略来源：deterministic / qwen / fallback
```

这里最核心的是 `passing_order` 和 `slots`。它们不是日志字段，而是会进入底层 x 方向速度控制，决定后机什么时候允许靠近洞口。

### 5.2 蛇形时序穿越

当障碍宽度不能容纳完整编队，但能容纳单机时，算法采用蛇形时序穿越。

默认通过顺序采用中心优先：

```text
UAV 0 -> UAV 1 -> UAV 2 -> UAV 3 -> UAV 4
```

对于后续障碍，算法允许根据无人机到当前障碍的距离和对齐状态更新顺序。这个机制非常重要：穿越和绕行混合时，不应强行重新排队成原始队形，而应让“更接近、更早完成上一个障碍”的无人机优先进入下一个窄洞。

这就是当前算法中的“重新排时序，而不是重新排队形”。

### 5.3 时间槽

给定相邻无人机的时序间隔 `Delta t`，第 `i` 个通过的无人机获得：

```text
slot_i = [i * Delta t, (i + 1) * Delta t]
```

时间槽的作用不是让无人机机械等待固定秒数，而是作为“预计到达障碍中心的时空约束”。如果后机预测会过早到达，就降低 x 方向速度；如果前机已经释放且间距安全，则后机继续推进。

### 5.4 x 方向防追尾机制

多无人机时序穿越最容易出问题的地方是：后机在 x 方向追上前机。当前算法通过时空槽和前后机清障状态限制 x 方向速度：

```text
if rear_uav_predicted_arrival < front_uav_release_time + slot_interval:
    rear_uav_speed_x = limited_speed
else:
    rear_uav_speed_x = nominal_speed
```

这保证了无人机不是“看见洞口就一起冲”，而是在同一个窄洞前形成时空队列。

该机制也是稳定 demo 能够工作的关键。一旦新 demo 修改底层执行参数或绕过这个约束，就会出现后机撞前机、挤洞、时序穿越失效等问题。

## 6. 动态孔洞穿越执行

### 6.1 预测洞口中心

动态孔洞的中心随时间变化。无人机不能只对齐当前洞口位置，而应对齐预计穿越时刻的洞口位置。

算法以障碍运动模型预测：

```text
predicted_center_y = f(t + time_to_reach_gate)
```

然后为每架无人机构造横向和高度目标：

```text
target_y = smooth_blend(original_y, predicted_center_y, progress_to_gate)
target_z = smooth_blend(initial_z, pass_z, progress_to_gate)
```

其中 `progress_to_gate` 表示无人机从观察点到洞口之间的推进进度。越接近洞口，目标越强地收敛到洞口中心。

### 6.2 为什么不能在门内反复修正

开发过程中发现：如果无人机已经进入门体厚度范围，还继续大幅调整 y/z，就会产生两个问题：

1. 门体移动时无人机可能在门内横向修正，擦碰墙体。
2. 后续无人机会被堵在门口，导致整条时序队列崩溃。

因此当前执行逻辑强调：

```text
门前对齐
门内快速通过
门后释放
```

也就是说，y/z 对齐应主要发生在接近障碍之前；一旦满足安全对齐条件并进入穿越段，x 方向应保持足够推进速度，避免在门体内部犹豫。

### 6.3 近障保底机制

当无人机离障碍很近，但 z 方向尚未到达安全通过高度时，算法触发近障保底：

```text
if near_obstacle and vertical_not_safe:
    suppress_forward_motion
    keep_yz_alignment
```

该机制的作用是：宁可在障碍前短暂停住并完成高度对齐，也不带着错误高度硬冲。

需要注意的是，当前最终稳定版本保留的是关键的高度安全保底，而不是过度限制水平 y 方向。因为水平双重保险过强时，侧绕会被卡住；z 方向保底则能显著降低撞洞风险。

## 7. 实体绕行与顶部越障

### 7.1 实体障碍候选动作

实体障碍不是时序穿越问题，而是空间绕行问题。当前动作库主要包括：

```text
left_bypass    从左侧绕行
right_bypass   从右侧绕行
over_bypass    顶部越障
```

实体障碍目标点由障碍尺寸和无人机原始横向位置生成：

```text
侧绕目标 y = obstacle_center_y ± (size_y / 2 + safety_margin)
越顶目标 z = obstacle_top_z + safety_margin
清障 x = obstacle_x + size_x / 2 + release_margin
```

这里 `size_x` 的作用很关键。它表示障碍厚度，决定无人机必须飞到障碍后缘多远之后才能切换到下一个障碍。

### 7.2 侧绕速度问题

早期版本中，侧绕速度显得很慢，原因是 x 方向速度被 y 对齐误差过度拖慢。当前算法的执行原则是：

```text
未对齐时：允许 y 误差影响 x 前进速度，避免斜撞实体边缘
已对齐后：x 方向不再被 y 误差长期拖慢，应快速通过实体厚度
```

这使侧绕行为更接近真实飞行：先完成横向避障位置选择，然后沿 x 方向连续穿过障碍厚度，而不是在障碍侧边缓慢挪动。

### 7.3 穿越到绕行的过渡

穿越式障碍和绕行式障碍混合时，最容易出现模式切换混乱。当前算法的原则是：

```text
先穿过当前障碍的 UAV，可以先进入下一个障碍的绕行准备
未穿过当前障碍的 UAV，仍然受当前障碍时序穿越约束
```

这避免了“第一架无人机开始绕行后，后面无人机还没穿过窄洞就被绕行逻辑接管”的问题。

换句话说，过渡逻辑不是全队统一切换模式，而是按 UAV 个体的清障状态逐个切换。

## 8. LLM 高层策略层

### 8.1 LLM 的角色

LLM 在本算法中承担：

```text
语义理解
策略组合
可解释高层决策
在线局部重规划建议
```

LLM 不承担：

```text
电机控制
姿态控制
每帧速度控制
碰撞约束最终裁决
```

这一区分非常重要。论文中应明确表述：LLM 是语义策略生成器，不是低层控制器。

### 8.2 LLM 输入什么

LLM 接收结构化语义上下文，而不是仿真原始状态。输入包括：

```text
任务类型：multi_uav_dynamic_obstacle_passage
无人机数量
初始编队形式
队形宽度
单机安全宽度
参考前向速度
任务偏好
允许的策略模式：formation / snake_sequence / bypass
允许的目标策略：predictive_center_crossing / edge_bypass / hold_and_reform
障碍语义列表
确定性参考策略
输出格式约束
```

对于每个障碍，输入包括：

```text
障碍编号
功能类型：穿越型或实体型
x 位置
横向中心
通过高度
洞口宽度或实体尺寸
运动模型
运动幅值
风险等级
```

这能清楚回答：

```text
LLM 被输入了什么？
```

LLM 被输入的是由场景编码器构造的语义摘要，以及确定性规划器提供的可行参考策略。

### 8.3 LLM 输出什么

LLM 输出 JSON 格式的高层策略：

```text
plan_id
mode
passing_order
time_slot_interval
slots
obstacle_strategies
recover_after_last_obstacle
confidence
source
```

每个 `obstacle_strategy` 包括：

```text
obstacle_id
obstacle_index
mode
target_policy
obstacle_x
observe_x
clear_x
```

这里的输出是“可执行策略描述”，不是自然语言建议。自然语言解释用于展示，JSON 策略用于验证和执行。

### 8.4 为什么给 LLM 一个确定性参考策略

当前算法会把确定性基准策略一并放入 LLM 上下文。这不是作弊，而是工程上非常重要的约束提示。

原因有三点：

1. LLM 需要知道输出 schema，不应自由发挥字段。
2. 确定性策略提供物理可行的下界，LLM 可以在其基础上调整局部模式。
3. 如果 LLM 不确定，复制确定性策略比生成不可执行策略更安全。

因此 LLM 的价值不是凭空编造控制指令，而是在语义层对复杂障碍组合进行解释、选择和局部重规划。

## 9. 安全验证器

### 9.1 必要性

LLM 输出存在不确定性，不能直接进入飞控系统。安全验证器是 LLM 和底层执行器之间的硬边界。

验证器使系统满足：

```text
LLM 可以建议
验证器必须裁决
底层只执行验证通过的策略
```

### 9.2 验证内容

验证器检查：

```text
JSON 是否可解析
字段是否完整
passing_order 是否覆盖所有 UAV
passing_order 是否有重复或非法编号
slots 是否与 UAV 数量一致
slot 时间是否单调
obstacle_strategies 是否覆盖全部障碍
实体障碍是否使用 bypass
穿越型障碍是否使用合理的穿越策略
observe_x 是否位于障碍前
clear_x 是否位于障碍后
时间窗是否存在明显冲突
策略是否能被底层执行器消费
```

如果 LLM 输出不合法，系统不尝试强行执行，而是：

```text
使用确定性 fallback 策略
记录验证失败原因
在语义面板中显示 rejected / fallback
```

### 9.3 学术意义

安全验证器让本算法不同于普通“LLM 输出路径”的方案。它形成了一个可讨论的安全结构：

```text
LLM intelligence under deterministic safety constraints
```

也就是：LLM 提供语义智能，确定性模块提供安全边界。

## 10. 异步在线 LLM 重规划

### 10.1 为什么需要在线 LLM

最早的 demo 是仿真开始前一次性把全部障碍告诉 LLM。这可以验证整体策略链路，但与真实飞行还有距离。真实无人机通常只能在有限视野内逐步看到障碍，因此需要在线语义重规划：

```text
飞行过程中发现新障碍
构造局部语义上下文
触发 LLM 进行局部策略分析
将新策略合并到在线策略缓冲区
```

这样 LLM 不再只是开场前做一次分析，而是在飞行过程中随着感知事件参与策略更新。

### 10.2 为什么必须异步

如果同步调用 Qwen 或其他在线 LLM：

```text
控制循环 -> 等待 LLM 返回 -> 继续控制
```

会导致 viewer 和控制循环卡住数秒。真实飞行中这不可接受，因为飞控环必须持续运行。

因此当前采用异步在线重规划：

```text
发现新障碍
    ↓
立即生成确定性 fallback
    ↓
fallback 通过验证后立刻进入策略缓冲区
    ↓
后台线程提交 Qwen 请求
    ↓
主控制循环继续运行
    ↓
Qwen 返回策略
    ↓
验证器检查
    ↓
若通过，则合并进在线策略缓冲区
    ↓
若失败，则 fallback 继续执行
```

### 10.3 异步重规划的核心优点

#### 10.3.1 控制不中断

飞行控制和 viewer 渲染不会等待 LLM 网络响应。即使 Qwen 延迟几秒，底层也会继续执行已验证的 fallback 策略。

#### 10.3.2 LLM 不再承担实时安全责任

LLM 可以慢一点，但不能让系统失控。确定性 fallback 保证任何时刻都有可执行策略。

#### 10.3.3 更符合真实机器人系统架构

真实机器人通常采用：

```text
高频控制层：稳定、连续、低延迟
中频规划层：局部路径和避障
低频语义层：任务理解和策略更新
```

异步 LLM 正好对应低频语义层，避免把大模型放进高频飞控闭环。

#### 10.3.4 让 LLM 作用更容易展示

语义面板中可以清楚看到：

```text
发现障碍
fallback accepted
LLM request submitted
LLM pending
LLM completed accepted / rejected
strategy buffer updated
```

这比“开头调用一次 LLM”更能体现实时智能系统的高级感。

### 10.4 为什么 fallback 每次看起来很精准

当前 fallback 不是随机策略，也不是简单保守悬停。它来自同一套确定性语义规划器：

```text
语义编码
可通行性分析
时空槽分配
障碍 observe/clear 点生成
安全验证
```

也就是说，fallback 本身就是一个几何和时序上可执行的基准解。LLM 是在这个基准解上提供语义策略增强，而不是从零生成一切。

这种设计有两个好处：

1. LLM 失败时系统仍能跑通。
2. LLM 成功时可以比较它相对 fallback 的策略差异。

这也为后续论文实验提供了天然对照组。

## 11. 在线感知与策略缓冲区

### 11.1 有限视野

在线重规划不把全部地图一次性交给 LLM，而是基于前向感知窗口：

```text
可见但未揭示细节的障碍：只知道前方有障碍
进入详细感知范围的障碍：获得类型、尺寸、运动模型等语义
需要规划的障碍：加入局部 LLM 上下文
```

这使 demo 的逻辑更接近真实飞行：

```text
无人机飞到某一区域
感知系统识别前方障碍
语义编码器生成障碍摘要
LLM 或 fallback 生成局部策略
```

### 11.2 策略缓冲区

在线策略不是每次覆盖全局计划，而是合并到缓冲区中：

```text
planned_obstacle_ids：已规划但未清除的障碍
cleared_obstacle_ids：已清除的障碍
obstacle_strategies：当前可执行的障碍策略
accepted_plan_ids：已接受的策略编号
```

这样可以避免同一个障碍反复触发 LLM，也能防止已经通过的障碍继续影响无人机。

### 11.3 清障状态

每个障碍都有清障判据：

```text
穿越型障碍：UAV 通过洞口并到达释放点
实体型障碍：UAV 越过实体后缘并达到安全释放距离
```

只有满足清障条件后，该 UAV 才可以切换到下一个障碍策略。这个机制是解决混合障碍模式切换的关键。

## 12. 语义证据链可视化

### 12.1 为什么可视化是算法的一部分

导师关注的核心不是“终端打印很多文字”，而是要让 LLM 的介入过程直观可见。否则即使算法内部调用了 LLM，外部观看者也无法判断：

```text
LLM 到底看见了什么？
它被输入了什么语义？
它输出了什么策略？
系统有没有验证它？
底层执行是否真的采用了它？
```

因此当前语义证据链面板不是装饰，而是算法可解释性的一部分。

### 12.2 面板结构

当前面板将语义事件分为四类：

```text
1. 场景语义 / LLM 输入
2. LLM 宏观策略 / 安全验证
3. 底层执行反馈
4. 事件时间线
```

每类信息对应一段算法链路：

```text
场景语义：系统从环境中提取了哪些障碍语义
LLM 输入：大模型实际收到的结构化信息
LLM 输出：大模型生成的通行顺序、时间槽、障碍策略
安全验证：策略是否可执行、是否 fallback
执行反馈：无人机当前处于穿越、绕行、越顶、等待或恢复编队
时间线：整个语义决策链按时间顺序滚动
```

### 12.3 面板展示的算法价值

该面板把 LLM 的抽象推理过程转化为可观察的“证据链”：

```text
感知到障碍
    ↓
提取语义
    ↓
发送给 LLM
    ↓
LLM 输出策略
    ↓
验证器裁决
    ↓
策略进入缓冲区
    ↓
底层执行器执行
```

这直接回应导师提出的“思维链体现”要求。严格说，论文中不应声称展示了 LLM 内部不可见的真实思维链，而应表述为：

```text
semantic evidence chain
```

即语义证据链。它展示的是系统可记录、可验证、可复现实验链路，而不是虚构模型内部想法。

这个表述更严谨，也更适合论文写作。

## 13. 稳定执行内核

### 13.1 执行阶段

稳定执行内核将任务分为：

```text
rotate          起始转向
formation       编队接近障碍区
snake           障碍区内时序通行
reform          通过后恢复编队
post_formation  恢复后巡航
complete        任务完成
```

其中 `snake` 阶段并不只表示穿越窄洞，而是整个障碍区内的局部通行状态，包括穿越、绕行、越顶和等待。

### 13.2 执行器输入

执行器不直接读取 LLM 原始文本，而读取验证后的 `PassagePlan` 和在线策略缓冲区：

```text
当前 UAV 状态
当前障碍策略
passing_order
time_slot_interval
observe_x
clear_x
动态障碍预测信息
实体障碍尺寸信息
```

### 13.3 执行器输出

执行器最终给每架无人机生成连续目标：

```text
target_x
target_y
target_z
reference_speed_x
```

然后由现有无人机控制器跟踪目标位置和速度。

### 13.4 为什么要抽象稳定内核

当前已经将稳定底层执行抽象为独立内核，后续新 demo 不应随意修改它。原因是：

```text
稳定内核是算法主闭环
复杂场景应通过配置和语义层扩展
底层控制频繁修改会污染基准结果
论文实验需要一个固定可复现版本
```

这也是当前项目从“不断调 demo”转向“算法框架”的关键一步。

## 14. 当前稳定 demo 的完整闭环

稳定 demo 的障碍结构可以抽象为：

```text
动态孔洞 1
动态孔洞 2
动态孔洞 3
实体侧绕障碍
动态孔洞 4
实体越顶障碍
动态孔洞 5
```

该场景已经覆盖：

```text
动态穿越
蛇形时序
实体侧绕
顶部越障
穿越-绕行混合切换
在线感知
异步 LLM 重规划
安全验证
fallback
语义证据链可视化
最终恢复编队
```

因此它不是单一避障 demo，而是一个多模块闭环验证场景。

## 15. 学术创新点

### 15.1 语义驱动的混合障碍建模

传统避障通常直接在几何空间规划轨迹。本算法将障碍抽象为带功能属性的语义对象：

```text
可穿越
需绕行
可越顶
动态移动
高风险窄洞
厚实体障碍
```

这使 LLM 能够参与复杂障碍组合策略，而不是只处理自然语言命令。

### 15.2 多无人机时空协同通行

算法不是让每架无人机独立避障，而是构造多机时空槽，形成“谁先过、谁后过、后机何时能进”的协同机制。

该机制尤其适合动态窄洞、窄门和单机可通过但编队不可通过的场景。

### 15.3 LLM under verification

LLM 输出必须通过安全验证器才能执行。该结构提升了 LLM 机器人系统的可信度：

```text
LLM 负责语义策略
验证器负责安全边界
执行器负责连续控制
```

### 15.4 异步在线重规划

异步机制使 LLM 能够参与在线决策，同时不阻塞控制循环。这一点比“仿真开始前问一次 LLM”更接近真实无人机系统。

### 15.5 语义证据链可解释展示

系统不仅能飞，还能展示：

```text
输入语义
LLM 输出
验证结果
策略是否采用
底层执行状态
```

这对导师展示、论文答辩和后续投稿都非常重要，因为它让 LLM 的作用可见、可验证、可复现。

## 16. 与常见方法的区别

### 16.1 与传统规划方法

传统方法如人工势场、速度障碍、采样规划、MPC 等在几何约束明确的场景中有效，但遇到“窄洞可穿、实体可绕、宽体可越顶、不同障碍混合”的复杂语义组合时，往往需要大量人工规则。

SV-STCP 的优势是：

```text
先将障碍转化为语义对象
再由 LLM 参与策略组合
最后用确定性验证和控制保证安全
```

### 16.2 与纯 LLM 机器人方法

纯 LLM 方法容易出现：

```text
输出不可执行
实时性不足
缺少安全验证
难以和飞控闭环对接
```

SV-STCP 的区别是：

```text
LLM 不直接控制
LLM 输出结构化策略
策略必须验证
在线调用异步执行
失败时 fallback 保底
```

### 16.3 与强化学习方法

强化学习可以学习复杂策略，但通常需要大量训练数据，并且可解释性较弱。SV-STCP 当前更强调：

```text
可解释语义策略
可验证执行边界
小样本场景迁移
人能理解的决策链路
```

## 17. 建议实验设计

后续论文实验应围绕“证明每个模块确实有贡献”展开。

### 17.1 主实验

在稳定动态混合障碍场景中统计：

```text
任务成功率
总通过时间
最小机间距
最小障碍净空
时间槽等待次数
fallback 使用次数
LLM 响应延迟
策略验证通过率
```

### 17.2 消融实验

建议至少设置：

```text
无 LLM：只使用确定性策略
同步 LLM：控制循环等待 LLM
异步 LLM：当前方法
无验证器：LLM 输出直接采用
无时间槽：蛇形穿越不做时序约束
无近障保底：取消 z 方向安全停等
无语义面板：只输出终端日志
```

重点证明：

```text
异步 LLM 提升实时性
验证器提升安全性
时间槽提升多机通行稳定性
语义面板提升可解释性和展示效果
```

### 17.3 场景泛化实验

逐步增加复杂度：

```text
单一动态窄洞
多动态窄洞
穿越 + 实体侧绕
穿越 + 越顶
穿越 + 侧绕 + 越顶混合
有限视野在线发现
动态实体与动态孔洞混合
```

不要一开始堆非常复杂的 16 个障碍。更合理的论文路线是逐级增加复杂度，展示算法边界如何扩展。

## 18. 当前不足与后续方向

当前算法主线已经成型，但仍有工程和论文层面的不足：

```text
复杂实体形状目前仍主要映射为侧绕或越顶
多孔墙、底穿、斜洞、弯曲通道还需要扩展动作库
LLM 策略与底层动作库之间需要更系统的接口定义
在线 LLM 延迟需要统计并形成实验曲线
还需要更多随机场景验证鲁棒性
```

下一阶段不建议继续频繁修改稳定 demo 底层，而应：

```text
固定稳定 demo
补齐消融实验
记录统计指标
扩展可执行动作库
再逐步增加复杂障碍语义
```

## 19. 阶段性结论

当前算法已经从 demo 调参进入算法框架阶段。

已经完成的核心闭环包括：

```text
语义场景编码
可通行性分析
确定性时空策略
LLM 高层策略生成
安全验证与 fallback
有限视野在线感知
异步在线 LLM 重规划
稳定多机执行内核
动态孔洞蛇形穿越
实体侧绕和越顶
混合障碍平滑过渡
语义证据链可视化
```

最适合对外表达的算法主线是：

```text
一种面向动态混合障碍区的多无人机语义验证时空协同通行方法。
该方法通过语义场景编码将复杂障碍转换为 LLM 可理解的结构化描述，
通过 LLM 生成高层通行策略，
通过安全验证器约束策略合法性，
通过异步在线重规划避免大模型推理阻塞控制循环，
最终由稳定执行器完成多无人机蛇形穿越、实体绕行和越顶。
```

这套表述既体现了 LLM，又没有把安全责任错误地交给 LLM；既有算法创新点，也有工程落地性；既能支撑 demo 展示，也能发展成论文中的方法章节。
